/* 
When combining a String with a Number data type, the Number will be coerced into a String.
This means that what is within the console.log() will become '5' + '10' essentially
and therefore output '510' as a string due to string concatenation.
*/