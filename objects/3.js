let myArray = {
  0: 'zero',
  1: 'one',
  2: 'three',
  length: 3,
};

for (let i = 0; i < myArray.length; i += 1) {
  console.log(myArray[i]);
}

/*
This one definiteely had me stumped for a bit but I'd say mainly just because
I didn't understand what the question was really asking for in the first place.
The hint helped though.
*/
