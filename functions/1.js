/*
It should log 1 to the console due to the bar variable being declared in a global scope, and again
within the function using a local scope. When the log method is used it see the global scope bar which
was unchanged.
*/
